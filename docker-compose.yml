services:
  todolist:
    container_name: todolist
    image: todolist
    build: 
      context: .
    env_file: 
      - .env
    command: >
      sh -c "uv run flask db upgrade && uv run gunicorn todolist:app -w 2 -b :8000"
    ports:
      - "8000:8000"
